# initialize git repository
git init

# check the status of file
git status

#configuration of email and username
git config user.name "yourusername"
git config user.email "youremail@gmail.com"

#staging the files
git add filename, filename2
# or 
git add .

# one -time step(adding remote to your git)
git remote add origin "http://url_of_your_git_repo.git"

#commit the changes [important]
git commit -m "any essage stating for what this commit is"

#choose branch 
git checkout master
#or 
git checkout -b nayi_branch

#push the chnages to your branch
git push origin nayi_branch

#####
# pull the content from other branch
git pull origin master
#merge the changes in current branch
git margin

#####
#fetch all the tree structure form remote 
git fetch 